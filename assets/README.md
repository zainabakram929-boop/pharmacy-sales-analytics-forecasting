# Dashboard screenshots

Add sanitized exports from the Power BI dashboards here, for example:

- `churn_dashboard.png`

The original PBIX files are excluded from the public repository because Power BI files may contain embedded source data.
